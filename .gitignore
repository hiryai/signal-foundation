# Local plan files — not part of the shipped repo
docs/plans/

# Local build artifacts — discovery files, review files, scratch scripts written during /code-foundations:building runs
docs/building/

# OS noise
.DS_Store

# Local environment
.env
.env.local
.env.*.local

# Member-private filled brain — bootstrapped from brain.md.template on first /setup-brain run
brain/brain.md
